# Correlation Playbooks

## Execution to Network
Trigger when high risk process creation is followed by outbound connection.

## Execution to Persistence
Alert when a suspicious process creates registry run key.

## Multi Stage Attack
Office app -> script engine -> network -> persistence
